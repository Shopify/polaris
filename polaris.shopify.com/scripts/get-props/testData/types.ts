/**
 * A basic type
 */
type StringProps = string;

/**
 * A more complex type
 */
type TypeProps = {
  a: string;
  b: number;
};

/**
 * A function type
 */
type MyFunction = (a: string, b: number) => void;
