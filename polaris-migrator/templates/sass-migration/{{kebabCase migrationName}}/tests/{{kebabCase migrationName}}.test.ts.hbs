import {check} from '../../../utilities/testUtils';

const migration = '{{kebabCase migrationName}}';
const fixtures = ['{{kebabCase migrationName}}'];

for (const fixture of fixtures) {
  check(__dirname, {
    fixture,
    migration,
    extension: 'scss',
  });
}
