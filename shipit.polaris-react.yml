ci:
  require:
    - 'Test (16.13.0)'
    - 'Accessibility test'

merge:
  require:
    - 'Test (16.13.0)'
    - 'Accessibility test'

dependencies:
  override:
    - yarn install
  post:
    - yarn workspace @shopify/polaris build

deploy:
  override:
    # - yarn workspace @shopify/polaris publish --tag latest --access public
    - cd ./polaris-react
    - npm publish --tag latest --access public
