ci:
  require:
    - 'Test (16.13.0)'
    - 'Accessibility test'

merge:
  require:
    - 'Test (16.13.0)'
    - 'Accessibility test'

dependencies:
  override:
    - yarn install
  post:
    - yarn run build

deploy:
  override:
    - yarn workspace @shopify/polaris publish
