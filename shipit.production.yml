ci:
  require:
    - krate/lockfile-upload

production_platform:
  application: polaris
  runtime_ids:
    - production-unrestricted-x6he

dependencies:
  override:
    - yarn install
    - yarn build
