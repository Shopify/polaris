production_platform:
  application: polaris-shopify-com
  runtime_ids:
    - production-unrestricted
ci:
  require:
    # - Build
    # - buildkite/polaris-shopify-com-builder
    - krate/lockfile-upload
dependencies:
  override:
    - yarn install
  post:
    - yarn run build
