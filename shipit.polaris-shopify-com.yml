ci:
  require:
    - buildkite/polaris-react-production-builder
    - krate/lockfile-upload
production_platform:
  application: polaris-react
  runtime_ids:
    - production-unrestricted-3217
dependencies:
  override:
    - yarn install
  post:
    - yarn run build
