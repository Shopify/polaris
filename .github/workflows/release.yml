name: Release

on:
  push:
    branches:
      - main

concurrency: ${{ github.workflow }}-${{ github.ref }}

jobs:
  release:
    name: Release
    runs-on: ubuntu-latest
    env:
      RELEASE_BRANCH: 'refs/heads/changeset-release/main'
    steps:
      - name: Checkout Repo
        uses: actions/checkout@v2

      - name: 'Log branch and env info'
        run: |
          echo "github.ref: ${{ github.ref }}"
          echo "RELEASE_BRANCH: ${{ env.RELEASE_BRANCH }}"

      - name: Setup Node.js
        uses: actions/setup-node@v2
        with:
          node-version: 16.x

      - name: Install dependencies
        run: yarn --frozen-lockfile

      - name: Run preversion scripts
        if: ${{ github.ref == env.RELEASE_BRANCH }}
        run: yarn preversion-packages

      - name: Commit preversion updates
        if: ${{ github.ref == env.RELEASE_BRANCH }}
        uses: stefanzweifel/git-auto-commit-action@v4
        with:
          commit_message: Apply preversion changes

      - name: Create release Pull Request or publish to NPM
        id: changesets
        uses: changesets/action@v1
        with:
          publish: yarn release
        env:
          NPM_TOKEN: ${{ secrets.NPM_TOKEN }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
