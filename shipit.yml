ci:
  require:
    - 'Test'
    - 'Accessibility test'
    - 'Visual regression test'
  hide:
    - shrink-ray
    - percy/polaris-react
  allow_failures:
    - 'buildkite/polaris-react-system-integration-test'

merge:
  require:
    - 'Test'
    - 'Accessibility test'

deploy:
  max_commits: false
